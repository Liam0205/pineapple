{
  "description": "Operator referenced more than once in pipeline tree",
  "config": {
    "pipeline_config": {
      "operators": {
        "copy_op": {
          "type_name": "transform_copy",
          "direction": "common_to_item",
          "$metadata": {
            "common_input": ["tag"],
            "common_output": [],
            "item_input": [],
            "item_output": ["tag"]
          }
        }
      }
    },
    "pipeline_group": {"main": {"pipeline": ["copy_op", "copy_op"]}},
    "flow_contract": {
      "common_input": ["tag"],
      "item_input": ["id"],
      "common_output": [],
      "item_output": ["id", "tag"]
    }
  },
  "expected_error": {
    "type": "ConfigError",
    "message_contains": "more than once"
  }
}
