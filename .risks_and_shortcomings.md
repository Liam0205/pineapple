# 项目风险与短板（临时工作文件，不提交）

## 1. server 包覆盖率（10.6% → 52.3%）

- `handleHealth`: 0% → **100%** ✅
- `handleExecute`: 0% → **85.7%** ✅
- `writeJSON`: 0% → **100%** ✅
- `handleStats`: 0% → **100%** ✅
- `handleDAG`: 0% → **100%** ✅
- `Run`: 0% — 含 log.Fatal，不宜直接单测
- `watchConfig`: 0% — 无限循环，不宜直接单测

**状态**: 已解决（handler 全覆盖，Run/watchConfig 排除）

## 2. transform 包 Redis 算子覆盖率（56.6% → 88.8%）

- `redis_get.go`: Init **100%**, Execute 全路径 ✅
- `redis_set.go`: Init **100%**, Execute 全路径 ✅
- `size.go`: Init **100%** ✅
- `buildKeySuffix`: **100%** ✅

**状态**: 已解决（引入 miniredis 做内存 Redis 测试）

## 3. 无生产验证

7 天项目，无真实流量验证。~~DAG 并行调度、Lua pool 高并发、配置热重载延迟均为未知数。~~

**观测能力已补齐**：调度器（run count / peak concurrency / per-operator exec/skip/error/duration）、Lua pool（borrow/return/create/active）、配置热重载（count/error/duration）三维度均有 atomic 内置计数器 + 可选 Prometheus Provider 双通道覆盖。`/stats` 端点提供零配置基线，`pkg/metrics.Provider` 接口支持生产级 Prometheus 导出。

**状态**: 观测侧已解决；真实流量验证仍为长期风险

## 4. Python 侧端到端集成测试

**状态**: 已解决 ✅ — `apple/tests/test_e2e.py` 已有 4 个 E2E 测试，含完整 DSL → JSON → Go Engine 链路（`test_go_engine_executes_dsl_json`），全部通过。

## 5. 错误处理策略未系统化

**状态**: 已解决 ✅ — `design_doc/07_error_handling.md` 已补齐：类型化错误层次、fail_on_error 降级模式、Warning 收集路径、DAG 中止机制、HTTP 错误映射、Apple 编译期错误、请求级 contract 校验。
